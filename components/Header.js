export default function({ children }) {
  return (
    <p
      style={{
        fontSize: '24px',
        position: 'absolute',
        marginTop: '-72px'
      }}
    >
      {children}
    </p>
  )
}
